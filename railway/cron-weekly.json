{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile" },
  "deploy": {
    "startCommand": "atalaya init-db && atalaya collect-weekly",
    "cronSchedule": "0 13 * * 5",
    "restartPolicyType": "NEVER"
  }
}
